                    <li class="dropdown">
                        <a href="{{route('item_stock_alert_notification')}}">
                            <i class="material-icons">notifications</i>
                             @if($total_notification>0)
                                <span class="label-count">{{$total_notification}}</span>
                             @endif    
                        </a>
                    </li>

               

                   